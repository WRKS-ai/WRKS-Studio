---
// About Bill — "Meet your mentor" section. WRKS template: reads copy
// from content.aboutBill (see ../../data/content.schema.ts).
import Signature from '../Signature.astro';
import { content } from '../../data/content.sample';
const about = content.aboutBill;
---
<section class="bf-section">
  <div class="bf-container">
    <div class="bf-about-grid">
      <div class="bf-about__media">
        <img src={about.photo.src} alt={about.photo.alt} />
        <Signature class="bf-about__sig" />
      </div>
      <div class="bf-about__copy">
        <span class="bf-eyebrow">{about.eyebrow}</span>
        <h2 class="bf-h2">{about.heading}</h2>
        {about.paragraphs.map((p, i) => (
          <p style={i === 0 ? 'margin-top:28px' : undefined} set:html={p}></p>
        ))}
        <div class="bf-about__actions">
          <a class="bf-btn bf-btn--primary" href={about.cta.href}>{about.cta.label}</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Last <main> section before the footer — owns its 100px bottom pad so the
     white section breathes before the dark footer (the footer no longer adds
     outer spacing of its own). */
  section.bf-section { padding-bottom: var(--bf-section-pad); }
  /* Signature overlaid on the bottom-right of the photo. */
  .bf-about__media { position: relative; }
  .bf-about__media :global(.bf-about__sig) {
    position: absolute;
    right: 18px;
    bottom: 16px;
    width: 150px;
    max-width: 44%;
    color: #0a0a0a;
    filter: drop-shadow(0 1px 2px rgba(255, 255, 255, 0.55));
  }
  .bf-about__actions {
    margin-top: 28px;
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }
  /* New photo is portrait — frame it to a controlled height and keep Bill's
     head in view (top-weighted crop) so it doesn't dominate the section. */
  .bf-about__media img {
    height: 480px;
    object-fit: cover;
    object-position: center 18%;
  }
  @media (max-width: 980px) {
    .bf-about__media img { height: 320px; }
  }
</style>
